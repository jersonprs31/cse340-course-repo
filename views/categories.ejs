<%- include('partials/header') %>
    <main>
        <h1><%= title %></h1>
        <p>Explore our different areas of service:</p>
        <ul>
            <% categories.forEach(category => { %>
                <li><strong><%= category.category_name %></strong></li>
            <% }); %>
        </ul>
    </main>
<%- include('partials/footer') %>